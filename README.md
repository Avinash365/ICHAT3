A Real time Chat App, stack used MERN | JWT, Scoket.io 

# Setup .env file  
        PORT=...
        MONGO_DB_URI=...
        JWT_SECRET=...
        NODE_ENV=... 
# Build the app 
        npm run build 
# Start the app 
        npm start 
        
